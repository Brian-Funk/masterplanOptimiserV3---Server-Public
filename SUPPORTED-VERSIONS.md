# Supported versions

| Release | Security support |
| --- | --- |
| Latest signed release | Supported |
| Older releases | Unsupported, upgrade required |
| Unsigned test deployment | Development use only |

Security fixes are released as a new canonical `vMAJOR.MINOR.PATCH` tag. A GitHub Release title may use normal prose, but the tag is the release identity.
